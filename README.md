# Git Project 2

Created From Local System.

Used: Git Init
    Add files
    git add .
    git commit -m
    ** git remote add origin <- Link ->
    check git remote -v
    check git branch
    ** git push origin main.